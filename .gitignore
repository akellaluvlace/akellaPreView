# deps
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env
.env*.local
.env.local

# typescript
*.tsbuildinfo
next-env.d.ts

# vercel
.vercel

# ingestion clone cache (uiverse-io/galaxy + markmead/hyperui)
/.cache

# Claude Code workspace — local-only agent settings + per-session artifacts
.claude/

# IDE clutter
*.swp
*.swo
.idea/
.vscode/

# Working-tree screenshots (Windows + macOS naming conventions)
Screenshot*.png
screenshot-*.png

# Local-only diff previews & one-off scratch
phase5-prettier-diff-preview.txt
*.tmp
*.log

# Internal planning / audit / handoff docs — kept LOCAL only, never shipped
# to the public GitHub repo or the Vercel deploy. Covers CLAUDE.md +
# archives, AUDIT-*, phase*, plan, manipulation, playbooks, etc. README.md
# is the one root doc we keep as the repo's front page.
/*.md
!/README.md
/docs/
/audit-2026-05-04/
/audit-2026-05-06/

# One-off scratch scripts used during refactors
/_*.cjs
